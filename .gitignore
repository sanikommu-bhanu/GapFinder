node_modules/
.next/
out/
build/
dist/

# Legacy local SQLite file — the app uses Postgres now, but old checkouts may
# still have one lying around.
prisma/dev.db
prisma/dev.db-journal

# Env
.env
.env.local

# Uploaded work is stored in the database, not on disk — this directory is a
# leftover from the previous filesystem-backed implementation.
public/uploads/*
!public/uploads/.gitkeep

# Misc
.DS_Store
*.pem
npm-debug.log*
.vercel
*.tsbuildinfo
