node_modules/
.next/
out/
build/
dist/

# Legacy SQLite file from before the move to Postgres.
prisma/*.db
prisma/*.db-journal

# Env
.env
.env.local

# Uploaded work lives in the database, never on disk — a serverless host has
# no persistent filesystem to write to.

# Misc
.DS_Store
*.pem
npm-debug.log*
.vercel
*.tsbuildinfo
